# JobRadar Frontend Environment Variables
# Copy to .env.local for local development

# Backend API URL — leave empty to use the Vite dev proxy (npm run dev) or the
# nginx proxy (Docker). Both route /api to the backend, so you normally leave this blank.
VITE_API_URL=
